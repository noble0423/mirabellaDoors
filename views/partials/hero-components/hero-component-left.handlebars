<div class="container-fluid px-0 vw-100">
    <div class="col-12 px-0 hero-component-left-img-div shadow {{ heroComponentLeft.animation }}" style="background-image: url('{{ heroComponentLeft.image.src }}');" title="{{ heroComponentLeft.image.alt }}">
        <div class="row">
            <div class="col-1"></div>
            <div class="col-4">
                <div class="hero-component-left-text-div mx-4 px-4 shadow">
                    <h2 class="display-5 text-center my-6">{{ heroComponentLeft.title }}</h2>
                    <h4 class="text-center text-muted"><em>Write something here like a tagline, selling point, business stat, or something to intro the page.</em></h4>
                </div>
            </div>
            <div class="col-7"></div>
        </div>
    </div>
</div>